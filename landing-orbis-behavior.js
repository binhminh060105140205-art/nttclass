(function(){var page=document.getElementById('landingPage');if(!page)return;var videos=[].slice.call(page.querySelectorAll('video'));function sync(){var visible=!page.classList.contains('hidden')&&!document.hidden;videos.forEach(function(video){if(visible){var play=video.play();if(play&&play.catch)play.catch(function(){})}else video.pause()})}page.querySelectorAll('a[href^="#orbis"]').forEach(function(link){link.addEventListener('click',function(event){var target=document.querySelector(link.getAttribute('href'));if(target){event.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'})}})});var observer=new MutationObserver(sync);observer.observe(page,{attributes:true,attributeFilter:['class']});document.addEventListener('visibilitychange',sync);window.addEventListener('beforeunload',function(){observer.disconnect()},{once:true});sync()})();
